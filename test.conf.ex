[test]
# Change the following parameters to the values for your RHEV test environment
# and then copy this file to test.conf.
#username = <username>
#domain = <domain>
#password = <password>
#datacenter = Default
#cluster = Default
#template = Blank
#livecd = <iso>
#
# This is an optional setting. If you set it, the API ill be tested at this
# location. If it is unset, a local instance of the API is fired up.
#url = http://rhevm.example.com/api
